.gnocchi-text-wrapper
  @extends $container
  position relative

.gnocchi-text
  @extends $reset
  @extends $container
  @extends $box
  @extends $space
  @extends $typography
  @extends $focus

  &--has-counter &
    padding-right 45px

  &::placeholder
    color #b9b9b9
