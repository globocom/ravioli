.gnocchi-text-wrapper
  @extends $container
  position relative

.gnocchi-text
  @extends $reset
  @extends $container
  @extends $box
  @extends $space
  @extends $typography
  @extends $focus

  &::placeholder
    color #b9b9b9
